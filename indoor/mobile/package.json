{
  "name": "dont-run-indoor-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor Android shell for indoor subway navigation. Wraps pipeline/review_ui + src/location with native Wi-Fi scanning.",
  "scripts": {
    "prepare:data": "node scripts/bundle-data.mjs",
    "sync": "npm run prepare:data && npx cap sync android",
    "open:android": "npx cap open android",
    "run:android": "npx cap run android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
